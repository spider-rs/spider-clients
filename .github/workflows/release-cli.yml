name: Release CLI (crates.io)

# Publishes the spider-cloud-cli crate. The CLI depends on the published
# `spider-client` crate, so release `rust-v*` BEFORE `cli-v*`.

on:
  push:
    tags:
      - "cli-v*.*.*"

permissions:
  contents: read

jobs:
  publish:
    name: Publish spider-cloud-cli to crates.io
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Verify tag matches Cargo.toml version
        working-directory: ./cli
        run: |
          PKG_VERSION="$(grep -m1 '^version' Cargo.toml | sed -E 's/.*"([^"]+)".*/\1/')"
          TAG_VERSION="${GITHUB_REF_NAME#cli-v}"
          echo "Cargo.toml=$PKG_VERSION tag=$TAG_VERSION"
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "::error::Tag $GITHUB_REF_NAME ($TAG_VERSION) does not match Cargo.toml ($PKG_VERSION)"
            exit 1
          fi

      - name: Build
        run: cargo build --release
        working-directory: ./cli

      - name: Publish
        run: cargo publish --token "$CARGO_REGISTRY_TOKEN"
        working-directory: ./cli
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
